You can run my application after you installed all requirements using
- pip install -r requirements.txt
Then run project using 
- uvicorn app.main:app --reload
You can test application using
- pytest -v app/tests
